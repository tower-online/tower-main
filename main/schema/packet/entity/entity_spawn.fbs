include "entity_types.fbs";

namespace tower.net.packet;

table EntitySpawn {
    entity_type: EntityType;
    entity_id: uint32;
    position: Vector2;
    rotation: float32;
}

root_type EntitySpawn;