{
  "dockerComposeFile": [
    "../compose.yaml",
    "compose.extend.yaml"
  ],
  "service": "main",
  // "mounts": [
  //   "source=${localWorkspaceFolder}/main,target=/app,type=bind,consistency=cached"
  // ],
  "workspaceFolder": "/app",
  "shutdownAction": "stopCompose"
}